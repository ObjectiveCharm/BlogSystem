## General Configuration 
1. Use English for comments
2. NO emojis in comments, commit messages
3. 添加测试到 Blog.Tests/XxTests' 下，增量添加，绝对绝对不要破坏项目结构，不要破坏文件结构

## Backend
### Database
1. Use PostgreSQL as the database, connect with string "Host=localhost:5433;Database=blog;Username=alfheim_admin;password=password"

## Dependencies
1. Use pgroonga for full-text search
2. Install dependencies should be compatible with dotnet 9.0